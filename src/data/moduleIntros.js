// Module intro cards — the teaching layer shown before a module's first
// challenge. 3 short concept cards per module, EN/PT, terminal-styled.
// Rendered by <ModuleIntroOverlay> in PunkSQL.jsx (reuses onboarding chrome).

export const MODULE_INTROS = {
  1: [
    { t_en: "TABLES = ROWS × COLUMNS", t_pt: "TABELAS = LINHAS × COLUNAS",
      b_en: "A database stores data in tables —\nlike spreadsheets.\n\n customers\n ┌────┬───────────┬─────────┐\n │ id │ name      │ country │\n ├────┼───────────┼─────────┤\n │ 1  │ Ana Silva │ Brazil  │\n │ 2  │ John Smith│ USA     │\n └────┴───────────┴─────────┘\n\nEach ROW is one record.\nEach COLUMN is one attribute.",
      b_pt: "Um banco de dados guarda dados em\ntabelas — como planilhas.\n\n customers\n ┌────┬───────────┬─────────┐\n │ id │ name      │ country │\n ├────┼───────────┼─────────┤\n │ 1  │ Ana Silva │ Brazil  │\n │ 2  │ John Smith│ USA     │\n └────┴───────────┴─────────┘\n\nCada LINHA é um registro.\nCada COLUNA é um atributo." },
    { t_en: "SELECT ASKS A QUESTION", t_pt: "SELECT FAZ UMA PERGUNTA",
      b_en: "SQL reads almost like English:\n\n SELECT name, country\n FROM customers;\n\n= \"give me the name and country\n   of every customer\"\n\nSELECT → which columns\nFROM   → which table\n*      → shorthand for ALL columns",
      b_pt: "SQL se lê quase como uma frase:\n\n SELECT name, country\n FROM customers;\n\n= \"me dê o nome e o país\n   de cada cliente\"\n\nSELECT → quais colunas\nFROM   → qual tabela\n*      → atalho para TODAS as colunas" },
    { t_en: "LIMIT + DISTINCT", t_pt: "LIMIT + DISTINCT",
      b_en: "Two survival tools:\n\n SELECT * FROM products LIMIT 5;\n → only the first 5 rows\n   (always use it when exploring)\n\n SELECT DISTINCT category\n FROM products;\n → each category once, no repeats\n\nNow try it for real →",
      b_pt: "Duas ferramentas de sobrevivência:\n\n SELECT * FROM products LIMIT 5;\n → só as 5 primeiras linhas\n   (use sempre ao explorar)\n\n SELECT DISTINCT category\n FROM products;\n → cada categoria uma vez\n\nAgora pratique de verdade →" },
  ],
  2: [
    { t_en: "WHERE FILTERS ROWS", t_pt: "WHERE FILTRA LINHAS",
      b_en: "WHERE keeps only rows that pass\na condition:\n\n SELECT * FROM customers\n WHERE country = 'Brazil';\n\nThe condition is checked on\nEVERY row — matching rows stay,\nthe rest disappear.\n\nText needs 'quotes', numbers don't.",
      b_pt: "WHERE mantém só as linhas que\npassam numa condição:\n\n SELECT * FROM customers\n WHERE country = 'Brazil';\n\nA condição é testada em CADA\nlinha — quem casa fica,\no resto some.\n\nTexto usa 'aspas', números não." },
    { t_en: "COMBINE CONDITIONS", t_pt: "COMBINE CONDIÇÕES",
      b_en: " AND → both must be true\n WHERE cat = 'Food' AND price < 20\n\n OR → either one is enough\n WHERE status = 'pending'\n    OR status = 'shipped'\n\n IN → shortcut for many ORs\n WHERE status IN ('pending','shipped')",
      b_pt: " AND → as duas precisam valer\n WHERE cat = 'Food' AND price < 20\n\n OR → uma delas basta\n WHERE status = 'pending'\n    OR status = 'shipped'\n\n IN → atalho para vários OR\n WHERE status IN ('pending','shipped')" },
    { t_en: "PATTERNS + NULL", t_pt: "PADRÕES + NULL",
      b_en: " LIKE matches patterns:\n   '%son'  ends with \"son\"\n   'A%'    starts with \"A\"\n   %       = any characters\n\n NULL means \"unknown\" — it is NOT\n zero and NOT empty text:\n   WHERE email IS NULL\n   WHERE email IS NOT NULL\n (col = NULL never works!)",
      b_pt: " LIKE casa padrões:\n   '%son'  termina com \"son\"\n   'A%'    começa com \"A\"\n   %       = qualquer coisa\n\n NULL significa \"desconhecido\" —\n NÃO é zero nem texto vazio:\n   WHERE email IS NULL\n   WHERE email IS NOT NULL\n (col = NULL nunca funciona!)" },
  ],
  3: [
    { t_en: "ORDER BY SORTS THE RESULT", t_pt: "ORDER BY ORDENA O RESULTADO",
      b_en: " SELECT * FROM products\n ORDER BY price;\n\n→ cheapest first (ASC is default)\n\n ORDER BY price DESC;\n→ most expensive first\n\nSorting happens AFTER filtering —\nWHERE first, then ORDER BY.",
      b_pt: " SELECT * FROM products\n ORDER BY price;\n\n→ mais barato primeiro (ASC é padrão)\n\n ORDER BY price DESC;\n→ mais caro primeiro\n\nOrdenar acontece DEPOIS de filtrar —\nprimeiro WHERE, depois ORDER BY." },
    { t_en: "TIES + MULTIPLE KEYS", t_pt: "EMPATES + VÁRIAS CHAVES",
      b_en: " ORDER BY category ASC,\n          price DESC;\n\nSorts by category first;\ninside each category, sorts\nby price (highest first).\n\nLeft to right = priority order.",
      b_pt: " ORDER BY category ASC,\n          price DESC;\n\nOrdena por categoria primeiro;\ndentro de cada categoria, ordena\npor preço (maior primeiro).\n\nEsquerda → direita = prioridade." },
    { t_en: "TOP-N = ORDER BY + LIMIT", t_pt: "TOP-N = ORDER BY + LIMIT",
      b_en: "The classic combo:\n\n SELECT name, price\n FROM products\n ORDER BY price DESC\n LIMIT 3;\n\n→ \"the 3 most expensive products\"\n\nOrder first, then cut.\nNow you try →",
      b_pt: "O combo clássico:\n\n SELECT name, price\n FROM products\n ORDER BY price DESC\n LIMIT 3;\n\n→ \"os 3 produtos mais caros\"\n\nOrdena primeiro, corta depois.\nAgora é com você →" },
  ],
  4: [
    { t_en: "AGGREGATES SUMMARIZE", t_pt: "AGREGAÇÕES RESUMEM",
      b_en: "Aggregate functions turn many\nrows into one number:\n\n COUNT(*)      how many rows\n SUM(price)    total\n AVG(price)    average\n MIN / MAX     extremes\n\n SELECT COUNT(*) FROM orders;\n → one row, one number.",
      b_pt: "Funções de agregação transformam\nvárias linhas em um número:\n\n COUNT(*)      quantas linhas\n SUM(price)    total\n AVG(price)    média\n MIN / MAX     extremos\n\n SELECT COUNT(*) FROM orders;\n → uma linha, um número." },
    { t_en: "GROUP BY MAKES BUCKETS", t_pt: "GROUP BY CRIA GRUPOS",
      b_en: " SELECT category, COUNT(*)\n FROM products\n GROUP BY category;\n\n→ one row PER CATEGORY, with the\n  count inside each bucket.\n\nRule: every column in SELECT is\neither grouped or aggregated.",
      b_pt: " SELECT category, COUNT(*)\n FROM products\n GROUP BY category;\n\n→ uma linha POR CATEGORIA, com a\n  contagem dentro de cada grupo.\n\nRegra: toda coluna do SELECT ou\nestá no GROUP BY ou é agregada." },
    { t_en: "HAVING FILTERS GROUPS", t_pt: "HAVING FILTRA GRUPOS",
      b_en: "WHERE filters rows BEFORE grouping.\nHAVING filters groups AFTER:\n\n SELECT category, COUNT(*) AS n\n FROM products\n GROUP BY category\n HAVING n >= 3;\n\n→ only categories with 3+ products.\n\nWHERE → GROUP BY → HAVING.",
      b_pt: "WHERE filtra linhas ANTES do grupo.\nHAVING filtra grupos DEPOIS:\n\n SELECT category, COUNT(*) AS n\n FROM products\n GROUP BY category\n HAVING n >= 3;\n\n→ só categorias com 3+ produtos.\n\nWHERE → GROUP BY → HAVING." },
  ],
  5: [
    { t_en: "WHY JOIN?", t_pt: "POR QUE JOIN?",
      b_en: "Data lives in separate tables:\n\n orders     customers\n ┌────┬────────┐  ┌────┬──────┐\n │ id │cust_id │  │ id │ name │\n │ 1  │  2 ────┼──┼→2  │ John │\n └────┴────────┘  └────┴──────┘\n\ncustomer_id points at customers.id\n— JOIN follows that arrow and\nglues the rows together.",
      b_pt: "Os dados vivem em tabelas separadas:\n\n orders     customers\n ┌────┬────────┐  ┌────┬──────┐\n │ id │cust_id │  │ id │ name │\n │ 1  │  2 ────┼──┼→2  │ John │\n └────┴────────┘  └────┴──────┘\n\ncustomer_id aponta para customers.id\n— o JOIN segue essa seta e cola\nas linhas." },
    { t_en: "INNER JOIN + ON", t_pt: "INNER JOIN + ON",
      b_en: " SELECT o.id, c.name\n FROM orders o\n JOIN customers c\n   ON c.id = o.customer_id;\n\nON says HOW rows match.\no / c are aliases — nicknames\nso you can write c.name.\n\nINNER = only rows that match\non both sides.",
      b_pt: " SELECT o.id, c.name\n FROM orders o\n JOIN customers c\n   ON c.id = o.customer_id;\n\nON diz COMO as linhas casam.\no / c são aliases — apelidos\npara escrever c.name.\n\nINNER = só linhas que casam\ndos dois lados." },
    { t_en: "LEFT JOIN KEEPS EVERYONE", t_pt: "LEFT JOIN MANTÉM TODOS",
      b_en: " FROM customers c\n LEFT JOIN orders o\n   ON o.customer_id = c.id\n\nKeeps EVERY customer — even ones\nwith no orders (their order\ncolumns come back NULL).\n\nUse it for \"who has none?\"\nquestions.",
      b_pt: " FROM customers c\n LEFT JOIN orders o\n   ON o.customer_id = c.id\n\nMantém TODO cliente — mesmo sem\npedidos (as colunas de pedido\nvoltam NULL).\n\nUse para perguntas tipo\n\"quem não tem nenhum?\"." },
  ],
  6: [
    { t_en: "A QUERY INSIDE A QUERY", t_pt: "UMA QUERY DENTRO DA OUTRA",
      b_en: " SELECT name FROM products\n WHERE price > (\n   SELECT AVG(price)\n   FROM products\n );\n\nThe inner query runs first\n(→ one number), then the outer\nquery uses it.\n\n\"Products above average price.\"",
      b_pt: " SELECT name FROM products\n WHERE price > (\n   SELECT AVG(price)\n   FROM products\n );\n\nA query interna roda primeiro\n(→ um número), depois a externa\nusa o resultado.\n\n\"Produtos acima do preço médio.\"" },
    { t_en: "IN (SUBQUERY)", t_pt: "IN (SUBQUERY)",
      b_en: "Subqueries can return a LIST:\n\n SELECT name FROM customers\n WHERE id IN (\n   SELECT customer_id\n   FROM orders\n   WHERE total_amount > 100\n );\n\n\"Customers who placed big orders.\"\nNOT IN gives you the opposite.",
      b_pt: "Subqueries podem retornar uma LISTA:\n\n SELECT name FROM customers\n WHERE id IN (\n   SELECT customer_id\n   FROM orders\n   WHERE total_amount > 100\n );\n\n\"Clientes com pedidos grandes.\"\nNOT IN dá o contrário." },
    { t_en: "EXISTS ASKS YES/NO", t_pt: "EXISTS PERGUNTA SIM/NÃO",
      b_en: " SELECT name FROM customers c\n WHERE EXISTS (\n   SELECT 1 FROM orders o\n   WHERE o.customer_id = c.id\n );\n\nEXISTS is true if the subquery\nfinds AT LEAST ONE row — it\nreferences the outer row (c.id),\nrunning once per customer.",
      b_pt: " SELECT name FROM customers c\n WHERE EXISTS (\n   SELECT 1 FROM orders o\n   WHERE o.customer_id = c.id\n );\n\nEXISTS é verdadeiro se a subquery\nacha PELO MENOS UMA linha — ela\nusa a linha externa (c.id),\nrodando por cliente." },
  ],
  7: [
    { t_en: "WINDOWS: AGGREGATE WITHOUT COLLAPSING", t_pt: "JANELAS: AGREGAR SEM COLAPSAR",
      b_en: "GROUP BY collapses rows.\nWindow functions DON'T:\n\n SELECT name, price,\n   AVG(price) OVER () AS avg_all\n FROM products;\n\nEvery row stays, and each one\ncarries the overall average\nnext to it.",
      b_pt: "GROUP BY colapsa linhas.\nWindow functions NÃO:\n\n SELECT name, price,\n   AVG(price) OVER () AS avg_all\n FROM products;\n\nToda linha permanece, e cada uma\ncarrega a média geral ao lado." },
    { t_en: "RANKING", t_pt: "RANKING",
      b_en: " ROW_NUMBER() OVER\n   (ORDER BY price DESC)\n → 1, 2, 3, 4…  (no ties)\n\n RANK() → 1, 2, 2, 4\n DENSE_RANK() → 1, 2, 2, 3\n\nPARTITION BY restarts the count\nper group:\n OVER (PARTITION BY category\n       ORDER BY price DESC)",
      b_pt: " ROW_NUMBER() OVER\n   (ORDER BY price DESC)\n → 1, 2, 3, 4…  (sem empates)\n\n RANK() → 1, 2, 2, 4\n DENSE_RANK() → 1, 2, 2, 3\n\nPARTITION BY reinicia a contagem\npor grupo:\n OVER (PARTITION BY category\n       ORDER BY price DESC)" },
    { t_en: "LAG / LEAD — LOOK AROUND", t_pt: "LAG / LEAD — OLHE AO REDOR",
      b_en: " LAG(total) OVER\n   (ORDER BY order_date)\n → the PREVIOUS row's value\n\n LEAD(...) → the NEXT row's\n\nPerfect for \"change since last\nmonth\" style questions:\n total - LAG(total) OVER (...)",
      b_pt: " LAG(total) OVER\n   (ORDER BY order_date)\n → o valor da linha ANTERIOR\n\n LEAD(...) → o da PRÓXIMA\n\nPerfeito para perguntas tipo\n\"variação vs mês passado\":\n total - LAG(total) OVER (...)" },
  ],
  8: [
    { t_en: "CTE = NAMED SUBQUERY", t_pt: "CTE = SUBQUERY COM NOME",
      b_en: " WITH big_orders AS (\n   SELECT * FROM orders\n   WHERE total_amount > 100\n )\n SELECT * FROM big_orders;\n\nWITH gives a query a NAME you\ncan reuse below — like a\nvariable for tables.",
      b_pt: " WITH big_orders AS (\n   SELECT * FROM orders\n   WHERE total_amount > 100\n )\n SELECT * FROM big_orders;\n\nWITH dá um NOME a uma query para\nreusar embaixo — como uma\nvariável de tabela." },
    { t_en: "CHAIN CTES = PIPELINE", t_pt: "CTES EM CADEIA = PIPELINE",
      b_en: " WITH step1 AS (...),\n      step2 AS (\n        SELECT ... FROM step1\n      )\n SELECT * FROM step2;\n\nEach CTE can read the previous\nones — complex logic becomes\nreadable steps instead of\nnested spaghetti.",
      b_pt: " WITH passo1 AS (...),\n      passo2 AS (\n        SELECT ... FROM passo1\n      )\n SELECT * FROM passo2;\n\nCada CTE pode ler as anteriores —\nlógica complexa vira passos\nlegíveis em vez de espaguete\naninhado." },
    { t_en: "WHEN TO REACH FOR A CTE", t_pt: "QUANDO USAR CTE",
      b_en: "Use a CTE when:\n· a subquery is used twice\n· the query has clear stages\n· you want to name an idea\n  (\"monthly_revenue\")\n\nThis is exactly how dbt models\nthink — every model is a named\nstep in a pipeline.",
      b_pt: "Use CTE quando:\n· uma subquery aparece 2×\n· a query tem etapas claras\n· você quer nomear uma ideia\n  (\"receita_mensal\")\n\nÉ exatamente como modelos dbt\npensam — cada model é um passo\nnomeado de um pipeline." },
  ],
  9: [
    { t_en: "READING vs WRITING", t_pt: "LER vs ESCREVER",
      b_en: "So far you only READ data.\nDML statements WRITE it:\n\n INSERT INTO t VALUES (...)\n UPDATE t SET col = x WHERE …\n DELETE FROM t WHERE …\n\n⚠ UPDATE/DELETE without WHERE\nhits EVERY row. Always write\nthe WHERE first.",
      b_pt: "Até agora você só LEU dados.\nDML ESCREVE:\n\n INSERT INTO t VALUES (...)\n UPDATE t SET col = x WHERE …\n DELETE FROM t WHERE …\n\n⚠ UPDATE/DELETE sem WHERE\natinge TODAS as linhas. Escreva\no WHERE primeiro." },
    { t_en: "DIRTY DATA IS NORMAL", t_pt: "DADO SUJO É NORMAL",
      b_en: "Real tables have problems:\nNULLs, negatives, duplicates.\n\nThe cleaning loop:\n 1. SELECT to FIND bad rows\n 2. UPDATE to fix — or\n    DELETE to remove\n 3. SELECT again to verify\n\nExplore before you mutate.",
      b_pt: "Tabelas reais têm problemas:\nNULLs, negativos, duplicatas.\n\nO ciclo de limpeza:\n 1. SELECT para ACHAR o ruim\n 2. UPDATE para corrigir — ou\n    DELETE para remover\n 3. SELECT de novo para conferir\n\nExplore antes de mudar." },
    { t_en: "SAFETY NET: SAVEPOINT", t_pt: "REDE DE SEGURANÇA: SAVEPOINT",
      b_en: " SAVEPOINT before_cleanup;\n DELETE FROM sales\n  WHERE quantity < 0;\n -- looks wrong?\n ROLLBACK TO before_cleanup;\n -- looks right?\n RELEASE before_cleanup;\n\nTransactions make destructive\nchanges reversible.",
      b_pt: " SAVEPOINT antes_limpeza;\n DELETE FROM sales\n  WHERE quantity < 0;\n -- ficou errado?\n ROLLBACK TO antes_limpeza;\n -- ficou certo?\n RELEASE antes_limpeza;\n\nTransações tornam mudanças\ndestrutivas reversíveis." },
  ],
  10: [
    { t_en: "DDL SHAPES THE DATABASE", t_pt: "DDL MOLDA O BANCO",
      b_en: "DML changes data;\nDDL changes STRUCTURE:\n\n CREATE TABLE events (\n   id INTEGER PRIMARY KEY,\n   name TEXT NOT NULL,\n   price REAL\n );\n\nEach column gets a TYPE and\noptional rules (constraints).",
      b_pt: "DML muda dados;\nDDL muda a ESTRUTURA:\n\n CREATE TABLE events (\n   id INTEGER PRIMARY KEY,\n   name TEXT NOT NULL,\n   price REAL\n );\n\nCada coluna tem um TIPO e\nregras opcionais (constraints)." },
    { t_en: "VIEWS = SAVED QUERIES", t_pt: "VIEWS = QUERIES SALVAS",
      b_en: " CREATE VIEW big_orders AS\n SELECT * FROM orders\n WHERE total_amount > 100;\n\n SELECT * FROM big_orders;\n\nA view stores the QUERY, not the\ndata — always fresh, zero copies.\nThis is what dbt models compile to.",
      b_pt: " CREATE VIEW big_orders AS\n SELECT * FROM orders\n WHERE total_amount > 100;\n\n SELECT * FROM big_orders;\n\nA view guarda a QUERY, não os\ndados — sempre fresca, sem cópia.\nÉ nisso que models dbt viram." },
    { t_en: "EVOLVE WITH ALTER", t_pt: "EVOLUA COM ALTER",
      b_en: " ALTER TABLE products\n   ADD COLUMN discount REAL;\n\n DROP TABLE old_stuff;\n DROP VIEW old_view;\n\nCTAS = create from a query:\n CREATE TABLE backup AS\n SELECT * FROM products;",
      b_pt: " ALTER TABLE products\n   ADD COLUMN discount REAL;\n\n DROP TABLE old_stuff;\n DROP VIEW old_view;\n\nCTAS = criar a partir de query:\n CREATE TABLE backup AS\n SELECT * FROM products;" },
  ],
  11: [
    { t_en: "WHAT IS dbt?", t_pt: "O QUE É dbt?",
      b_en: "dbt turns SQL SELECTs into a\nmanaged pipeline. Each file in\nmodels/ is one model:\n\n -- stg_orders.sql\n SELECT id AS order_id, …\n FROM {{ source('raw','orders') }}\n\ndbt runs it as CREATE VIEW/TABLE\n— in the right order, tested,\ndocumented.",
      b_pt: "dbt transforma SELECTs em um\npipeline gerenciado. Cada arquivo\nem models/ é um model:\n\n -- stg_orders.sql\n SELECT id AS order_id, …\n FROM {{ source('raw','orders') }}\n\nO dbt executa como CREATE VIEW/\nTABLE — na ordem certa, testado,\ndocumentado." },
    { t_en: "ref() BUILDS THE DAG", t_pt: "ref() MONTA O DAG",
      b_en: " -- fct_revenue.sql\n SELECT …\n FROM {{ ref('stg_orders') }}\n\nref() declares a dependency.\nFrom all refs, dbt draws the DAG:\n\n raw → stg_orders → fct_revenue\n\nand always builds upstream first.",
      b_pt: " -- fct_revenue.sql\n SELECT …\n FROM {{ ref('stg_orders') }}\n\nref() declara uma dependência.\nDos refs, o dbt desenha o DAG:\n\n raw → stg_orders → fct_revenue\n\ne sempre constrói upstream antes." },
    { t_en: "TRY IT FOR REAL", t_pt: "PRATIQUE DE VERDADE",
      b_en: "These challenges teach the\nsyntax. When you finish, open:\n\n HOME → FREE_EXPLORE → ▲ DBT\n\nA real dbt lab runs in your\nbrowser: edit models, dbt run,\ndbt test — plus graded missions\n(⚑) that earn XP.",
      b_pt: "Estes desafios ensinam a sintaxe.\nQuando terminar, abra:\n\n HOME → FREE_EXPLORE → ▲ DBT\n\nUm laboratório dbt real roda no\nseu navegador: edite models,\ndbt run, dbt test — e missões\navaliadas (⚑) que dão XP." },
  ],
};
